[build]
command = "npm run build"
functions = "netlify/functions"
publish = "dist/public"

[build.environment]
NODE_VERSION = "20"
NODE_ENV = "production"
NITRO_PRESET = "netlify"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
